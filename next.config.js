/** @type {import('next').NextConfig} */
const nextConfig = {
  // Minimal config to satisfy Vercel's strict Next.js detection
  // This project uses Vite + Express; Next.js is not actually used.
};

export default nextConfig;
